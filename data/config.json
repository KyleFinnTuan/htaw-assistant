{
    "token" : "${TOKEN}",
    "discordClientId": "${DISCORD_CLIENT_ID}",
    "mongoUrl": "${MONGO_URL}",
    "geminiApiKey": "${GEMINI_API_KEY}",

    "devToken": "${DEV_TOKEN}",
    "devDiscordClientId": "${DEV_DISCORD_CLIENT_ID}",
    "devGuildId": "${DEV_GUILD_ID}",
    "devMongoUrl": "${DEV_MONGO_URL}",

    "developerUserId": ["617374726654001172"]
}